# Shared proxy settings, included by each `location` in kianbehsaz.conf.
#
# Mounted at /etc/nginx/proxy_params.conf rather than under conf.d/, because
# nginx auto-includes every conf.d/*.conf at http level and this file is meant
# to be included explicitly.

proxy_http_version 1.1;
proxy_set_header Host              $host;
proxy_set_header X-Real-IP         $remote_addr;
proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
proxy_set_header X-Forwarded-Proto $scheme;
proxy_set_header CF-Connecting-IP  $remote_addr;
proxy_set_header Upgrade           $http_upgrade;
proxy_set_header Connection        "upgrade";

proxy_connect_timeout 10s;
proxy_send_timeout    60s;
proxy_read_timeout    60s;
